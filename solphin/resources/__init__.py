"""Data files bundled with solphin.

Kept as a real package (rather than a bare data directory) so that
``importlib.resources.files("solphin.resources")`` resolves identically from a
source checkout and from an installed wheel.
"""
